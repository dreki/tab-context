export interface ITab {
    id: number;
    title: string;
    // Group name and color are optional
    groupName?: string;
    groupColor?: string;
    url: string;
    favIconUrl?: string;
}

export { };
